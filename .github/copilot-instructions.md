# Copilot Instructions

## Project Guidelines
- User prefers API tests to be split into separate files per endpoint to keep test files smaller and easier to manage.
- User prefers domain tests in a dedicated .NET 10 test project using xUnit, NSubstitute, and FluentAssertions.
- User prefers infrastructure as code using OpenTofu and Terragrunt per environment for state management.